public interface IteratedFunction{ //a completely abstract class used to group related methods with empty bodies 
    public IteratedFunction next();
    public boolean equivalent(IteratedFunction node);
}